---
scope: P2 crm-targeting-lab 전체 시스템(CAP-1~8)
purpose: 배치 분석 파이프라인 + BI 소비층의 일관성 계약 — 두 데이터셋 격리와 데이터마트 경계가 핵심
altitude: 프로젝트(초기) → 에픽
updated: 2026-07-20T09:38
---

- (decision) 패러다임: 순수계산 분석모듈(crm/) + 얇은 오케스트레이션(pipelines/) — P1의 scorecard/+pipelines/ 조합 계승. Medallion 레이어링은 데이터 규모(10,127건) 대비 의식 과다이고 'gold에서 만나는 층'이 결합 유혹을 만들어 기각
- (decision) 두 데이터셋 격리 강제 수단(사용자 결정): 모듈 구조 + 산출물 스키마 고정 조합. 문서·리뷰 규칙만으로 두지 않음 — P1에서 AD-3(프레임 불변)/AD-9(의존 방향)가 매 리뷰 grep 대상으로 실제 값을 한 선례
- (constraint) BI 소비자(Tableau)는 우리가 제어하지 못하는 외부 도구 — 파이프라인 산출물이 아니라 고정 계약(데이터마트)만 노출해야 함. P1의 서빙 레이어 자리를 외부 도구가 차지하는 구조적 차이
- (decision) 데이터마트 2개 분리(사용자 결정): mart_customers.csv(BankChurners — 세그먼트·이탈확률·고객가치·4분면·타겟우선순위) / mart_ltv_demo.csv(Online Retail — BG/NBD 고객별 LTV). 한 행에 두 출처가 못 오는 것을 파일 경계로 물리 보장 — 모듈 격리 결정과 정합. Tableau는 고객분석 2탭↔mart_customers, LTV데모 1탭↔mart_ltv_demo로 '별도 전시관' 연결
- (decision) SPEC NFR-3 '단일 CSV/뷰' 문구는 이 결정으로 개정 대상 — 설계 결정 변경이므로 스펙 소급 개정(P1 2-2 선례: 리뷰 결과의 스펙 소급 반영)
- (decision) 4분면 판정 소유권(사용자 결정): 공식 라벨은 Python이 계산해 mart에 컬럼으로 고정 — Python/CSV/Tableau 어디서 보든 동일 분류 보장. Tableau의 인터랙티브 임계선은 '가상 시나리오 분석' 전용으로 역할 분리(공식 판정 아님). 즉 대시보드는 '기준을 바꾸면 어떻게 달라지는가'만 담당
- (question) 위 결정의 잔여 리스크: Tableau 시나리오 4분면과 공식 quadrant가 화면에서 혼동될 수 있음 — 컬럼명(quadrant_official 등)과 대시보드 사양서의 라벨 규칙으로 구분을 강제할지 결정 필요
- (decision) 가정 파라미터 단일 출처(사용자 결정): crm/config.py에 RETENTION_SUCCESS_RATE·COST_PER_CONTACT·RANDOM_SEED·경로 상수 집중. 계산 함수는 인자로 받되 기본값이 이 상수를 참조 — 상수는 한 곳, 테스트는 인자 주입으로 독립 검증. P1 scorecard/config.py 선례이자 P1 current_cutoff 이원화 사고(3-4 리뷰 deferred)의 재발 방지
- (decision) 공식/시나리오 4분면 구분을 이름으로 강제: 마트 컬럼은 quadrant_official, 대시보드 사양서는 인터랙티브 뷰에 '시나리오(공식 판정 아님)' 표기 의무. 화면에서 두 4분면이 구분 안 되면 분리 결정 자체가 무의미해짐
- (decision) RFM 산출 데이터셋 확정: CAP-1 RFM은 BankChurners에서 프록시로 계산(R=Months_Inactive_12_mon, F=Total_Trans_Ct, M=Total_Trans_Amt) — 세그먼트가 mart_customers의 컬럼이므로 2x2와 같은 모집단이어야 함. Online Retail의 진짜 거래로그 RFM은 CAP-4 LTV 데모 내부 전처리로만 사용하고 세그먼트로 승격하지 않음(격리 유지). RFM 프록시의 한계는 산출물에 명시
- (decision) 이탈확률·SHAP 단일 아티팩트 계약: mart_customers의 churn_prob와 CAP-3의 SHAP 해석은 반드시 동일 학습 아티팩트에서 산출. 모델 재학습 없이 SHAP만 따로 돌리는 경로 금지 — 확률과 설명이 어긋나면 리텐션 액션 근거가 무너짐(P1 AD-2 train/serve parity의 P2판)
- (revision 2026-07-22, 스토리 3-0) 위 결정의 `churn_prob`는 **두 컬럼으로 분리됨**: `churn_score`(raw out-of-fold, 순위 전용)와 `churn_prob_calibrated`(Platt 보정, 확률 해석 전용). 하나의 컬럼이 "순위"와 "확률" 두 역할을 겸하면서 실제로는 둘 다 아니었던 것(미보정 in-sample)이 원인이며, AD-5도 함께 개정됐다. 단일 아티팩트 계약 자체는 유지되나 **점수의 출처는 번들 안의 최종 모델이 아니라 그 형제인 폴드 모델들**이라는 단서가 붙는다(3-0 코드리뷰 지적, AD-5 보증 문구 축소 여부는 미결)
- (decision) 재현성 계약: crm/config.py의 RANDOM_SEED 단일 시드를 K-means(n_init 고정)·XGBoost·train_test_split이 공유. 동일 입력→동일 마트를 보장하고, 마트 산출 스크립트는 재실행 시 결정적으로 같은 파일을 생성
- (decision) 실행 envelope: 로컬 단일 환경 배치 실행만(API·서버·스케줄러 없음). python -m pipelines.NN_xxx 순차 실행으로 마트 생성 → Tableau가 마트 CSV를 파일로 연결. 컨테이너·클라우드·CI는 명시적 스코프 밖(P1 AD-8 동류)
- (decision) 데이터·아티팩트 gitignore + 재생성 스크립트(P1 NFR5 계승). 단 마트 CSV는 Tableau Public 연결·리뷰 재현을 위해 커밋 대상으로 예외 — 원본 데이터가 아닌 집계 산출물이고 크기가 작음(10,127행)
- (decision) 파이프라인 단계 계약: 01_download(두 데이터셋 각각) → 02_features(RFM·가치 프록시) → 03_train_churn(+SHAP) → 04_ltv_demo(별도 경로) → 05_marts(2개 CSV+스키마 문서). 각 단계는 파일 산출물로만 다음 단계와 통신(중간 상태 공유 금지)
- (constraint) 테스트 계약(SPEC NFR-4): RFM 계산·기대절감액 산식은 pytest 필수. 기대절감액은 P1 profit 산식처럼 행동 기반 테스트(동어반복 재구현 금지 — P1 2-2 points_lost 부호반전 교훈)
- (version) 웹 검증(2026-07-16): lifetimes는 PyPI 12개월간 신규 릴리스 없음 = Inactive/discontinued, 공식 후속은 PyMC-Lab/PyMC-Marketing. pymc-marketing 0.19.4(2026-05-06 릴리스, Production/Stable, PyMC Labs 지원, Python>=3.12, BG/NBD·Gamma-Gamma·Pareto/NBD 지원)
- (decision) CAP-4 LTV 라이브러리 교체: lifetimes -> pymc-marketing. 근거 ①lifetimes는 실측 discontinued이고 포트폴리오에서 죽은 라이브러리 사용은 감점 요인 ②PyMC-Marketing이 공식 후속이며 동일 모델(BG/NBD+Gamma-Gamma) 제공 ③Python 3.12 요건 충족. 비용: PyMC 기반이라 의존성이 무겁고 MCMC가 느림 — Online Retail 고객수(수천 규모)에서는 허용, 필요시 MAP 적합으로 완화. SPEC stack.md의 lifetimes 표기는 개정 대상
- (decision) CAP-4 폴백 유지: pymc-marketing 설치·적합 실패 시 코호트 기반 단순 LTV로 폴백(원 기획서 리스크 대응 승계). CAP-4는 OQ-1 결정으로 2x2와 분리된 방법론 데모라 폴백해도 핵심 산출물이 무너지지 않음
- (event) Reviewer Gate: lint 0건. 루브릭 워커 12건(High 3: mermaid linkStyle 오인덱싱·SPEC 델타 3종 미선언), 기술검증 5건(SHAP/pytest/pandas 버전 노후, Tableau Public 비공개 저장 불가=공개 노출, MAP 불확실성 구간 부재), 적대적 12건(핵심 진단: '금지는 고정했으나 정의는 미고정')
- (decision) 적대적 리뷰 반영: 스파인의 체계적 약점은 공유 표면의 정의 부재 — 마트 컬럼·고객가치 함수·4분면 규칙·target_priority 정렬·CSV 직렬화가 전부 미정의라 준수하면서 발산 가능. AD-11(가치 단일정의)·AD-12(판정규칙 단일소유)·AD-13(신선도·아티팩트 정체성·원자적 쓰기) 신설, AD-1/2/3/4/5/7/9 강화
- (decision) AD-7 결정론 전면 확장: 기존 3개 시드로는 불충분 — MCMC random_seed·SHAP 배경샘플링·XGBoost n_jobs 부동소수 축약순서·groupby sort·범주인코딩 순서·K-means 라벨 번호 불안정성이 전부 미고정이었음. 수용기준을 '2회 연속 실행 시 마트 바이트 동일'로 테스트화
- (decision) AD-1 격리 루프홀 4종 봉쇄: 공유 유틸의 fit 상태·config에 데이터 도출 상수 등재·하드코딩·05_marts가 두 프레임 동시 보유. 규칙 추가: 한 레인 데이터에서 계산된 값은 다른 레인 계산에 진입 금지, config는 정책가정만, 공유유틸은 stateless, 05는 순차처리
- (event) spine finalized — AD-1~13, lint 0건, 리뷰어 3종 지적 전량 반영(적대적 12건·루브릭 12건·기술검증 5건), SPEC 개정 3건 반영 완료
