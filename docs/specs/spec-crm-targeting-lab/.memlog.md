---
topic: P2 crm-targeting-lab — 카드고객 세그멘테이션·이탈예측·LTV·캠페인 타겟팅 CRM 분석 풀사이클 + Tableau
updated: 2026-07-20T09:37
---

- (note) 입력: ob_storage/신용평가_CRM_사이드프로젝트/02_CRM_타겟팅_랩.md (기획안+SRS+개발계획, 2026-07-10, FR-1~10/NFR-1~4) — 구조화 입력이라 express 증류
- (direction) P1(credit_scorecard-lab) 관례 계승: SPEC-first 경로, 정직성 톤(가정·한계 명시), deferred-work 기록, 실데이터 실행 DoD, 3-레이어 코드리뷰
- (capability) CAP-1 RFM 세그멘테이션: intent=RFM 지표+K-means로 고객군 분리(k 근거 elbow/실루엣), success=세그먼트 프로필 리포트+페르소나 4~6개 (FR-1,FR-2 통합)
- (capability) CAP-2 이탈예측: intent=XGBoost로 이탈확률 추정(불균형 scale_pos_weight, CV, PR-AUC 병행), success=baseline 로지스틱 대비 PR-AUC +15% 리프트 (FR-3,FR-4)
- (capability) CAP-3 이탈요인 해석: intent=SHAP 전역·개별 해석을 세그먼트별 리텐션 액션으로 번역, success=세그먼트별 이탈요인 top5+액션 매핑 문서 (FR-5)
- (capability) CAP-4 LTV 추정: intent=BG/NBD+Gamma-Gamma(lifetimes)로 12개월 기대 LTV, success=고객별 LTV 테이블 산출 (FR-6)
- (capability) CAP-5 2x2 타겟팅 매트릭스: intent=이탈위험xLTV로 Save우선/관망/저비용유지/이탈수용 4분면 분류, success=매트릭스 시각화+분면별 고객수·정책 제안 (FR-7)
- (capability) CAP-6 캠페인 시뮬레이터: intent=기대절감액=P(이탈)xLTVx성공률-비용으로 예산 제약 하 타겟 우선순위, success=예산 시나리오에서 무작위 타겟 대비 기대절감액 배수 제시 (FR-8)
- (capability) CAP-7 민감도 분석: intent=성공률·비용 그리드에서 ROI 등고선(오프라인 정책 평가), success=민감도 등고선 차트+해석 (FR-9, P2 우선순위)
- (capability) CAP-8 Tableau 대시보드: intent=세그먼트현황/이탈위험/타겟팅 3탭 BI 공개, success=Tableau Public 링크 공개+README 연결 (FR-10)
- (constraint) 이탈(BankChurners 10,127건)·LTV(Online Retail II 100만건) 두 데이터는 레코드 결합 불가 — 결합 시도 금지, 우회 방식은 OQ-1로 미결
- (constraint) 재현성: 시드 고정, features->train->simulate 파이프라인 스크립트화 (NFR-2, P1 NFR1 계승)
- (constraint) Tableau용 최종 데이터마트는 단일 CSV/뷰 + 스키마 문서 (NFR-3) — 대시보드가 파이프라인 중간산출물을 직접 읽지 않음(P1 AD-9 정신)
- (constraint) pytest: RFM 계산·기대수익 산식 단위 테스트 필수 (NFR-4)
- (constraint) 분석 가정(데이터 결합 불가, 성공률 가정치) 명시적 문서화 (NFR-1) — P1 정직성 원칙과 동일
- (constraint) 통화 규율(P1 3-4 리뷰 교훈): 금액은 데이터 원 통화 그대로 보고, 환산 금지. BankChurners는 통화 무단위(한도·잔액), Online Retail II는 GBP — 기획서의 '예산 1천만 원' 시나리오는 통화 정합 재정의 필요(OQ-4)
- (constraint) lifetimes 유지보수 중단 리스크: 검증된 버전 고정, 실패 시 코호트 기반 단순 LTV 폴백 (기획서 리스크 대응 승계)
- (question) OQ-1(구조적): 2x2 매트릭스의 LTV 축 — 기획서는 '세그먼트 매핑 가정+LTV 분포 샘플링'(합성값)이나, BankChurners 내부 변수(Total_Trans_Amt·Total_Revolving_Bal·Credit_Limit)로 실측 가치 프록시를 만들면 합성 없이 2x2 성립. BG/NBD는 Online Retail에서 방법론 데모로 분리 가능. 어느 쪽? (P1 reject inference급 근본 결정)
- (question) OQ-2: BankChurners Attrition_Flag는 사후 스냅샷 라벨 — 관측창/예측창 시점 정의가 없어 '예측'의 의미가 제한됨. 이 한계의 문서화 수위와 프레이밍(단면 분류 vs 시계열 예측 흉내 금지) 결정 필요
- (question) OQ-3: 캠페인 성공률·건당 비용 기본값의 출처/범위 — 실측 불가라 가정 파라미터인데 기본 시나리오 값을 무엇으로 제시할지(민감도 분석이 완충하지만 대표값은 필요)
- (question) OQ-4: 성공기준 '예산 1천만 원 시나리오'의 통화 — 데이터가 무단위/GBP라 원화 시나리오는 환산 가정 필요. 예산을 '캠페인 N건' 또는 데이터 통화로 재정의할지
- (question) OQ-5(외부 의존): Tableau Public 계정·설치 확보 여부 — P1 SAS와 동류의 사용자 의존. 미확보 시 CAP-8을 데이터마트+스키마 산출로 축소하고 퍼블리시만 사용자 몫으로 분업할지
- (assumption) Kaggle BankChurners·UCI Online Retail II 데이터셋 다운로드 가능(공개 데이터). 스택은 Python 3.12+pandas+sklearn+XGBoost+SHAP+lifetimes+pytest(전부 표준)
- (decision) 코드 위치 Desktop/crm-targeting-lab, 문서 구조는 P1 관례(docs/specs, docs/planning-artifacts, docs/implementation-artifacts) 미러
- (event) Self-validate Pass1(정합성): PASS — CAP-1~8 전부 intent+success 보유, intent는 WHAT(구현 지시는 stack.md), 제약 7개 전부 설계 구속력 있음, non-goal 4개 명시, success signal 4항목 측정 가능, ID 유일. 미확정 사안은 지어내지 않고 OQ-1~5로 격리
- (event) Self-validate Pass2(보존): PASS — 기획서 목표5·FR-1~10·NFR-1~4·데이터2종·차별점4·리스크3·성공기준3 전부 SPEC/companion에 안착(FR->CAP 매핑: 1,2->CAP-1 / 3,4->CAP-2 / 5->CAP-3 / 6->CAP-4 / 7->CAP-5 / 8->CAP-6 / 9->CAP-7 / 10->CAP-8). Wrapper-only 드롭: 2주 일정표(에픽 단계에서 재구성), 밸류업 툴체인 공유 언급
- (event) SPEC 초판 렌더 완료 — CAP 8개, companions: stack.md(스택·데이터·디렉토리), conventions.md(P1 계승 관례 9항). OQ 5건 미결(OQ-1이 최우선 구조 결정)
- (decision) OQ-1 해소(사용자, 옵션3): 2x2 LTV축은 BankChurners 내부 변수 실측 가치 프록시가 주축 — 합성 샘플링 배제. Total_Trans_Amt(연간 거래액)를 1차 가치 프록시로, Total_Revolving_Bal·Credit_Limit(활용도) 보조. BG/NBD+Gamma-Gamma(CAP-4)는 Online Retail II에서 방법론 역량 데모로 분리(2x2에 합성 주입 안 함). 이로써 2x2 전 축이 동일 데이터셋 실측 → P1 reject-inference급 합성 한계 회피
- (decision) OQ-1 후속: CAP-5 2x2는 BankChurners 단일 데이터셋 내 (이탈위험=CAP-2 예측확률) x (가치=Total_Trans_Amt 프록시)로 구성. CAP-4 LTV는 Online Retail II 별도 산출물(고객별 12개월 기대 LTV 테이블)로 유지하되 2x2와 결합하지 않음 — 두 산출물의 독립성을 문서에 명시
- (decision) OQ-2 해소(기본값, P1 정직성 톤): Attrition_Flag는 사후 스냅샷 단면 라벨임을 명시하고, 산출물 전반에서 '이탈 예측'이 아니라 '이탈 위험 분류(cross-sectional)'로 프레이밍. 관측창/예측창 시계열 구조를 흉내내지 않음 — 이 한계를 리포트·대시보드에 기록(NFR-1)
- (decision) OQ-3 해소(기본값): 캠페인 성공률·건당 비용은 실측 불가한 가정 파라미터 — 대표 시나리오는 리텐션 성공률 30%(업계 통용 보수값)·건당 비용은 데이터 통화 단위로 파라미터화하고, CAP-7 민감도 분석이 이 가정의 강건성을 담당. 대표값은 '가정'으로 명시, 정답 주장 안 함
- (decision) OQ-4 해소(P1 통화 교훈): 예산 시나리오는 원화 환산 폐기 — '캠페인 건수 N' 또는 데이터 원 통화(BankChurners는 무단위 화폐, Online Retail은 GBP)로 표현. 성공기준의 '예산 1천만원'을 '고정 예산 하 타겟 K명' 형태로 재정의(통화중립)
- (question) OQ-5 잔존(외부 의존, 사용자 확인 필요): Tableau Public 계정·데스크톱 설치 확보 여부. P1 SAS 선례대로 분업 설계 — 세션은 Tableau가 소비할 데이터마트(단일 CSV+스키마 문서)와 대시보드 사양서까지 산출, 실제 워크북 제작·퍼블리시는 사용자. 미확보여도 CAP-8의 데이터마트 산출은 blocking 아님
- (event) OQ-1~4 해소 완료(1 사용자결정+3 기본값), OQ-5만 잔존(외부 의존). SPEC 재도출 트리거
- (event) Self-validate 재도출 Pass1(정합성): PASS — CAP-1~8 ID 보존, CAP-4/5 intent가 OQ-1 결정 반영해 재작성(LTV=독립 데모, 2x2=BankChurners 실측 프록시), 제약 8개 전부 구속력(신규: 라벨 단면성 표기·통화중립·가정 파라미터 라벨링), non-goal 5개(신규 2: 합성 LTV 주입 금지·시계열 예측 금지), success signal 4항목 측정가능
- (event) Self-validate 재도출 Pass2(보존): PASS — 원 기획서 claim 전량 유지. 변경된 것은 '어떻게'뿐(LTV 합성 결합 -> 실측 프록시+데모 분리)이며 FR-6(BG/NBD)·FR-7(2x2) 모두 산출물로 살아있음. 기획서의 '세그먼트 매핑 가정' 우회책만 의도적 폐기(사용자 결정, 근거는 memlog 28-29)
- (decision) 아키텍처 스파인 결정에 따른 SPEC 개정 3건: ①제약 '데이터마트 단일화(단일 CSV)' -> '출처별 마트 2개 + 스키마'(단일 파일은 AD-1 격리를 산출물에서 무효화) ②CAP-4·제약의 lifetimes -> pymc-marketing(웹 검증: lifetimes 마지막 릴리스 2020-07 discontinued) ③CAP-8 3탭 -> 4탭(LTV 데모 별도 마트의 귀결)
- (constraint) 신규 제약(스파인 AD-10 유래): Tableau Public은 비공개 저장 불가 — 퍼블리시 시 마트 내용이 공개 조회 가능. 마트에는 공개 데이터 유래 값만 담고, 비공개 데이터 유입 시 로컬 Desktop으로 축소
- (event) 스파인 채택: ARCHITECTURE-SPINE.md(AD-1~13)를 companion으로 편입
